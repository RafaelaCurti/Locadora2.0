@model List<TIten>
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Painel/Views/Shared/_Layout.cshtml";
}
<h2>
    Reserva
</h2>
<div class="row" style="margin: 30px 0; text-align: right">
    <div class="col-lg-12 text-right">
        <a class="btn btn-primary btn-lg" data-toggle="tooltip" title="Cadastrar"
           href="@Url.Action(MVC.Painel.Itens.Cadastrar())">
            <i class="fas fa-plus"></i> Cadastrar
        </a>
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <table class="table table-bordered table-hover tablesorter">
            <thead>
                <tr>
                    <th class="header">
                        Número da Reserva
                    </th>
                    <th class="header">
                        Filmes
                    </th>
                    <th class="header">
                        Valor
                    </th>
                    <th class="header">
                        Quantidade
                    </th>
                    <th class="header">
                        Ação
                    </th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Count > 0)
                {
                    foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @item.Reservation.Id
                            </td>
                            <td>
                                @item.Movie.Id
                            </td>
                            <td>
                                @item.Value
                            </td>
                            <td>
                                @item.Quantity
                            </td>
                            <td class="text-center">
                                <a class="btn btn-primary btn-xs" data-toggle="tooltip" title="Editar"
                                   href="@Url.Action(MVC.Painel.Itens.Editar(item.Id))">
                                    <i class="fas fa-pencil"></i> Editar
                                </a>
                                <button type="button" class="btn btn-danger btn-xs ajax-modal" data-toggle="tooltip" title="Excluir"
                                        href="@Url.Action(MVC.Painel.Itens.Excluir(item.Id))">
                                    <i class="fas fa-trash"></i> Excluir
                                </button>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="15" class="text-center">
                            <strong>Nenhum registro encontrado</strong>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>